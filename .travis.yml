dist: trusty
sudo: false

language: rust
rust:
  - stable
  - nightly

# Dependencies of kcov
addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - binutils-dev
      - libiberty-dev
      - wget
      - clang-3.6
      - cmake
      - python
      - python-virtualenv

after_success: ci/after_success.sh

notifications:
  email:
    on_success: never
